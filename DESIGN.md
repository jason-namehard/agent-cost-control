# 真实多 Agent 工作流设计（聚焦真实，不堆壳）

> 背景：盲审发现旧工作流"三道闸"里两道是死代码、记账链失真、对标报告部分断言不可溯源。
> 本文不再对标"Claude Code 领先"之类的口号，只回答一个问题：**Hermes → opencode / codex / WB 这条真实链路，怎么设计才对得起"多 Agent 工作流"这五个字。**
>
> 真实执行器（截至 2026-08）：
> - **Hermes（小马）**：编排者，任务分解、路由、验收、记账、缓存策略
> - **opencode（OC）**：写代码 harness（读写文件、跑命令、跑测试）
> - **codex**：审计 harness（sandbox 只读，隔离执行；注：用户已定稿审计改用 deepseek harness，codex 默认不派）
> - **WB（WorkBuddy）**：另一台机器本地执行（Word COM / Windows 桌面 / 文件系统 / WPS）

---

## 一、六个真实问题，逐条给结论

### Q1 任务路由：什么活给谁？

**结论：路由看"任务性质 × 执行器差异化能力"，不看 harness 品牌。**

决策树（派活前自问，按序走）：

```
0. 这活我自己（小马）能不能 30 分钟内直接做完、且无需重读整个项目？
   → 能：自己做，不派。小任务派活 = 净亏损（agent 要重读项目上下文）。
   → 不能：继续 ↓
1. 要写/改代码、跑测试？
   → opencode（代码执行 harness，能读写文件）
2. 要审计/评审代码（只读、隔离）？
   → deepseek harness 或 codex（sandbox 只读）
3. 要动 Windows 本机（Word COM / WPS / 桌面 / 文件系统 / 打包）？
   → WB（这是 OC/codex 都没有的差异化能力，本地执行）
4. 长上下文探索 / 调研 / 对标？
   → 视任务性质，多数小马直接做或派 OC
```

**核心原则：每个执行器必须有"不可替代的差异化能力"，否则就是维护负担。**

### Q2 相同 LLM 不同 harness，值不值得部署？（敢于否决）

**结论：值得的前提是"能力差异化"，不是"harness 多"。**

诚实的判断标准（面试官如果问"你底层不都是 deepseek，搞这么多 harness 干嘛"，这样答）：

- **harness 不同，能力确实不同**——系统提示、工具集、执行模型、沙箱策略都不一样。同样 deepseek-v4-flash，OC 是"读改写文件"的代码 agent，codex 是"sandbox 只读"的审计 agent，WB 是"本机桌面/Office"的执行 agent。**这是真的，不是话术。**
- **但"值得部署"要看差异化能力是否真的被你的任务用到**：
  - opencode：写代码 → 必留
  - WB：Windows 本地执行（Word/WPS/桌面）→ 差异化最强，必留
  - codex：审计。**如果你已经改用 deepseek harness 审计，codex 的差异化还剩多少？** 若 codex 只用来做"和 OC 一样的写代码"，那就是**不值得**——砍掉，别为了"多一个 harness"而留。
- **敢于否决 = 诚实说"这个 harness 我部署过，但发现能力重叠，砍了"**。这比"我部署了 8 个执行器"更有专业说服力。

### Q3 任务粒度：什么级别才值得并行拆解？

**结论：并行拆解只对"无依赖 + 足够大"的模块划算，不是越小越好。**

- 你的判断对：模块化拆解后不同模块给不同 Agent 并行。但有个硬约束——
- **并行阈值**：单模块工作量 > 单个 agent 的「启动 + 读整个项目上下文」开销，才值得拆。否则并行是负优化（每个 agent 都要重读一遍项目，token 翻倍）。
- **有依赖的模块不能并行**：A 模块产出是 B 模块输入，必须串行或先建依赖图。
- **现实判断**：个人项目（调洪工具箱、即忆 APP）的规模，真正的并行价值在「多个独立任务」同时跑（写 A 模块 + 写 B 模块 + 调研 C），而不是「把一个大任务切成碎片并行」。后者多数时候得不偿失。

### Q4 怎么审核任务完成程度？（验收协议）

**结论：不信 agent 自报，三层验收，独立可复现。**

1. **产物存在**：文件真的写出来了（不是 agent 在任务单里写"✅已完成"就算数）。当前 `agent_batch.py collect` 只 grep 状态字符串，**没查产物文件**——要补。
2. **测试通过**：小马**独立复跑** brief 里写明的测试命令，核对预期输出。不信 agent 说"测试通过"。
3. **人工 review**：关键代码 / 决策点人工过目（架构选型、引入新依赖、安全相关）。

**落地**：brief 里必须写「验收标准 = 测试命令 + 预期结果」，回收时逐条核，不达标标 ❌失败只补缺。

### Q5 什么时候发信号要用户主动介入？

**结论：低风险自动流转，高风险强制人工闸。**

必须叫用户的 5 个信号：
1. **验收失败**（测试不过 / 产物缺失）
2. **watchdog 熔断**（卡死 / 超时 / 超预算）
3. **agent 请求澄清**（信息不足，别让 agent 瞎猜烧 token）
4. **关键决策点**（架构选型、技术路线、是否引入新依赖、是否动敏感数据）
5. **敏感操作**（删除、生产环境、权限变更、付费模型）

**落地**：派活 brief 里预先标注「以下点必须停下来问用户」，agent 命中即停。

### Q6 缓存命中率（你的核心洞察，完全成立）

**结论：你的判断是对的，而且这是整套工作流里最有技术含量的一点。**

- **CLI 派活 = 每次全新 session = 前缀缓存命中率≈0**。小修小改若重新 `opencode run` / `codex exec`，等于把整个项目上下文再全价读一遍——费用极高。
- **正确做法（三层）**：
  1. **初次派活交接后，用户接手会话**：后续小修小改用 `opencode -c <session-id>`（或交互式打开该 session）/ `codex --resume` 续接，复用前缀缓存。这是成本最优路径。
  2. **记录 session 句柄**：派活脚本把 session id 写进任务单，作为「可续接句柄」，用户一键续接。
  3. **小马续接**（降级方案）：小马在同一 session 上续发指令，省去用户操作——但受平台限制（见下）。
- **当前瓶颈**（SKILL.md 已诚实记录）：用户看不到小马通过 CLI 调起的 session，无法续接。这是平台能力问题，不是流程问题。

---

## 二、真正的专业壁垒（不是堆出来的"三道闸"）

旧工作流想用"三道闸"造壁垒，结果两道是死的。真正的壁垒是这三样，都靠**判断力**而非堆代码：

| 壁垒 | 内容 | 面试能讲出的"深度" |
|---|---|---|
| **任务路由判断** | 什么活自己做、什么活派谁、什么活不值得派 | "小任务派活是净亏损"的成本直觉 |
| **验收协议** | 三层独立验收，不信自报 | 质量控制的可复现性 |
| **缓存/会话生命周期** | session 句柄、续接、避免重读 | "缓存命中率≈0"的机制洞察 |

---

## 三、待落地清单（区分改代码 / 改流程 / 待平台）

### 改代码（本次可做）
- [ ] `agent_batch.py collect` 增加**产物文件存在性校验**（Q4-1）
- [ ] 派活脚本（watchdog / wb_fix_assign）派活后**输出续接命令** `opencode -c <sid>`（Q6-2）
- [ ] `wb_fix_assign.py` git 快照：对目标目录 `git init` 或删掉该宣传（已改 SKILL.md 提示）

### 改流程（方法论，写进 brief 模板）
- [ ] brief 模板加「验收标准 + 必须人工介入的点」两段（Q4/Q5）
- [ ] 派活前走 Q1 决策树，小任务不派
- [ ] codex 角色重新裁定：审计用 deepseek harness，codex 若无差异化就停用（Q2）

### 待平台能力
- [ ] 用户可续接 CLI 调起的 agent session（Q6 瓶颈，需 Hermes/opencode 支持会话句柄透传）

---

## 四、一句话定位（简历可用的诚实版本）

> 不造"又一个 Agent 框架"，而是给 **Hermes + opencode + codex + WB** 这条真实链路，加上三样东西：**任务路由判断、独立验收协议、会话缓存生命周期管理**。核心不是"部署了多少 harness"，而是"知道什么活不值得派、怎么验、怎么续接省钱"。
